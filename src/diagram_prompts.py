"""
Prompts for diagram processing.

- DIAGRAM_PROMPT, STRICT_DIAGRAM_PROMPT: used by diagram_extractor (image -> text).
- GENERATE_PROMPT: used by diagram_generator (text -> PlantUML).
"""

# Diagram prompt
DIAGRAM_PROMPT = """Обязательно: всегда выдавай ответ. Пустой вывод не допускается.

Задача: извлечь из диаграммы текст алгоритма/процесса. Ответ — только на русском.

Верни ТОЛЬКО один из двух форматов (без markdown, без пояснений, без вступлений):

1) Если на диаграмме есть дорожки (swimlanes) с подписями ролей:
Шаг | Роль
1. <дословная подпись внутри фигуры> | <дословная подпись дорожки>
2. ...

2) Если дорожек нет:
Шаг
1. <дословная подпись внутри фигуры>
2. ...

Правила:
- Пиши только то, что НАПИСАНО на диаграмме (в фигурах и на дорожках). Никаких фактов/шагов/ролей "по смыслу".
- Текст шага — дословно из подписи фигуры (прямоугольник/ромб/событие с подписью). Не используй шаблоны/скобки вроде [Инициализация], [Выбор], и не перефразируй.
- Роль у шага — только подпись дорожки. Если дорожек нет — колонку «Роль» не выводи вообще.
- Формат должен быть консистентным: если заголовок «Шаг | Роль» — то КАЖДАЯ строка шага обязана содержать « | <роль>». Если дорожек нет — никакого символа «|» в ответе быть не должно.
- Стрелки/связи: перечисляй узлы по направлению процесса (как идут стрелки). При ветвлении: сначала одна ветка до конца, затем другая, затем общее продолжение.
- Конечные события без подписи не выводи как шаг.
- Если подпись не читается — напиши НЕРАЗБОРЧИВО (не придумывай).

Если на изображении нет пошагового процесса (не BPMN/flowchart) — тогда вместо форматов выше:
Описание
<кратко перечисли, что видно на изображении, без выдуманных деталей>"""


# Strict prompt
STRICT_DIAGRAM_PROMPT = """Верни ответ строго в одном из форматов. Никаких других заголовков/фраз.

Формат A (есть swimlanes):
Шаг | Роль
1. <подпись фигуры> | <подпись дорожки>
2. ...

Формат B (swimlanes нет):
Шаг
1. <подпись фигуры>
2. ...

Формат C (это не процесс):
Описание
<кратко что видно>

Проверка перед ответом:
- Первая строка должна быть РОВНО: «Шаг», «Шаг | Роль» или «Описание».
- Если выбрал «Шаг | Роль» — в каждой строке шагов должен быть символ «|» и роль.
- Если текста не видно — «НЕРАЗБОРЧИВО» (не придумывай)."""


# Generation prompt
GENERATE_PROMPT = """По описанию алгоритма ниже сгенерируй ТОЛЬКО код PlantUML для activity-диаграммы.
Выводи ТОЛЬКО код. Никаких пояснений, комментариев или текста до/после кода. После @enduml — ничего.

СИНТАКСИС (строго соблюдай):

1. Обёртка: @startuml ... @enduml
   Пиши @enduml с символом @ (собака), НЕ :enduml

2. Начало и конец: start — первая строка после @startuml, stop — обязательно перед @enduml

3. Активность (шаг): :подпись;
   НЕ используй: step "X", step X, или другие формы — только :подпись;

4. Ветвление if/then/else (строгий формат):
   if (условие?) then (подпись_да)
       :активность в ветке да;
   else (подпись_нет)
       :активность в ветке нет;
   endif

   Важно:
   - Условие в круглых скобках с ? в конце: (условие?)
   - then (подпись) — подпись для ветки «да» в скобках
   - else (подпись) — подпись для ветки «нет» в скобках
   - Обязательно endif в конце блока
   - НЕ пиши: then (, else (, if condition? then — это неверно
   - Запрещено: if condition? then ( — условие должно быть в скобках: if (condition?) then (да)

5. Параллельные ветки:
   split
       :активность 1;
   split again
       :активность 2;
   end split

6. Пустая ветка: используй :; (двоеточие-точка с запятой)

7. Включай ВСЕ шаги из описания. Не пропускай ни одного. Язык подписей — русский.

Пример (два шага подряд):
@startuml
start
:Шаг один;
:Шаг два;
stop
@enduml

Пример (с ветвлением):
@startuml
start
:Шаг один;
if (условие?) then (да)
    :Шаг два;
else (нет)
    :Шаг три;
endif
stop
@enduml"""
